name: Live Stats Update

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}
  push:
    branches:
      - main

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate live stats SVG
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USERNAME: saimdinky
        run: node scripts/generate-stats.js

      - name: Convert SVG to PNG for GitHub README
        run: |
          sudo apt-get update
          sudo apt-get install -y librsvg2-bin python3-pil
          rsvg-convert -w 1400 dist/live-stats.svg -o dist/live-stats.png
          python3 scripts/crop-stats-png.py

      - name: Commit and push if the stats changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/live-stats.svg dist/live-stats.png
          if git diff --staged --quiet; then
            echo "No change in stats, skipping commit."
          else
            git commit -m "chore: refresh live stats"
            git push
          fi
